<section>
    <header>{{user.name}}</header>

    <div>
      <p>{{user.name}} Dashboard</p>
    </div>
</section>
<a href="/">Home</a>
<section>
    {{#each blogs as |blog| }}
<div>
  <div>
    <h2>{{blog.title}}</h2>
  </div>
</div>
{{/each}}
</section>